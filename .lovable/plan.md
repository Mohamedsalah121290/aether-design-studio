
# خطة تطوير المنصة - الحالة الحالية

## ✅ تم إنجازه

### 1. ربط Stripe ✅
- إنشاء Edge Function `create-checkout` لإنشاء جلسات دفع Stripe
- إنشاء Edge Function `check-subscription` للتحقق من حالة الاشتراك
- إنشاء Edge Function `customer-portal` لإدارة الاشتراكات
- تحديث CheckoutDialog للتوجيه إلى Stripe Checkout
- إضافة أعمدة Stripe لجدول orders (stripe_session_id, payment_status)

### 2. جداول قاعدة البيانات ✅
- جدول `subscriptions` مع RLS
- جدول `courses` مع RLS
- جدول `lessons` مع RLS
- جدول `user_progress` مع RLS

## 🔲 المهام المتبقية

### 3. إشعارات البريد الإلكتروني
- ربط Resend لإرسال إيميلات
- إيميل تأكيد الشراء
- إيميل تفعيل الطلب

### 4. Academy Backend الديناميكي
- لوحة إدارة للأدمن لإضافة/تعديل الدروس
- ربط صفحة Academy بقاعدة البيانات
- نظام تتبع تقدم المستخدم

### 5. صفحة بروفايل المستخدم
- عرض بيانات المستخدم
- تاريخ المشتريات
- إدارة الاشتراكات

### 6. تفعيل تلقائي للطلبات
- Stripe Webhook لتحديث الطلبات تلقائياً
- تفعيل تلقائي بعد الدفع
